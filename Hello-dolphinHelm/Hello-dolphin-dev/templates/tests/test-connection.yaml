apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Hellocockatiel.fullname" . }}-test-connection"
  labels:
    {{- include "Hellocockatiel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Hellocockatiel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
